---
description: Set Peek logging to default — show status line only
allowed-tools: ["Bash"]
---

# Peek Logging Default

Set logging to default mode. Shows the memory injection status line (count and categories) but hides individual memory details.

```bash
node "${CLAUDE_PLUGIN_ROOT}/dist/commands/loggingDefault.js"
```

Report the result to the user.
