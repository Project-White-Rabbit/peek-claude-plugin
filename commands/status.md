---
description: Check Peek authentication and connection status
allowed-tools: ["Bash"]
---

# Peek Status

Run the status script to check if Peek is configured and connected.

```bash
node "${CLAUDE_PLUGIN_ROOT}/dist/commands/status.js"
```

Report the result to the user.
